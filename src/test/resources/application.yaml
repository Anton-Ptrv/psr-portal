spring:
  datasource:
    url: jdbc:postgresql://localhost/psr_test
    username: postgres
    password: password

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true